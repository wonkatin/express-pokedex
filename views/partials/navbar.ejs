<nav>
    <div class="left">
        <a href="/">Homepage</a>
        <a href="/pokemons">Pokemon</a>
    </div>
    <div class="right">
        <% if(user) { %>
            <a href="/users/<%= user.id %>"><%= user.username %></span>
            <a href="/auth/logout">Log Out</a>    
        <% } else { %>
            <a href="/auth/login">Log In</a>
            <a href="/auth/new">Sign up</a>
        <% } %>
    </div>
</nav>